# 79523 在线对战平台 — 设计文档

## 元信息

- **项目：** 79523 在线对战平台（手机网页版）
- **日期：** 2026-06-05
- **阶段：** 原型开发（单人）
- **下一步：** writing-plans 输出实现计划

---

## 1. 技术栈

| 层 | 技术 | 理由 |
|---|---|---|
| 前端 | Vue 3 + Vite + Pinia | SFC 直观，Composition API 灵活，Vite 热更新快 |
| 通信 | Socket.io | 自动处理重连、房间、广播，比裸 WebSocket 省代码 |
| 后端 | Node.js + Express + Socket.io | 一种语言两端复用类型 |
| 游戏状态 | 进程内存 | 原型阶段一台机器足够，后续加 Redis 成本低 |

不做：NestJS（太重）、Go（两套语言）、Redis/数据库（先跑通核心玩法）

---

## 2. 项目结构（Monorepo）

```
79523/
├── packages/
│   ├── engine/          纯函数库，零网络依赖
│   ├── server/          Express + Socket.io
│   └── client/          Vue 3 + Vite
├── package.json         pnpm workspace root
└── pnpm-workspace.yaml
```

---

## 3. 游戏规则（engine 层定义）

### 牌序

```
7 > 9 > 5 > 2 > 3 > A > K > Q > J > 10 > 8 > 6 > 4
```

花色：♠ > ♥ > ♣ > ♦

### 牌型（5 种）

| 牌型 | 英文 | 组成 | 张数 | 比大小规则 |
|------|------|------|------|-----------|
| 单张 | Single | 单独一张 | 1 | 比牌点，同点比花色 |
| 对子 | Pair | 两张相同 | 2 | 比对子牌点 |
| 单车 | Bike | 对子 + 任意单张 | 3 | 比对子牌点 |
| 三条 | Triple | 三张相同 | 3 | 比三条牌点 |
| 根号 | Root | 两对 + 任意单张 | 5 | 比最大的对子 |

**牌型隔离：** 必须跟牌型，不同牌型之间不能互压。

### 计分

- 5 = 5 分
- 10 = 10 分
- K = 10 分
- 其他牌不计分

### 牌数

- < 4 人：1 副牌（52 张，无鬼牌）
- 4-6 人：2 副牌（104 张，无鬼牌）

---

## 4. 游戏流程

### 一局

1. **发牌：** 每人 5 张
2. **首出：** 持有最小单张牌的玩家先出
3. **循环：** 出牌 → 判定 → 补牌 → 下家
4. **结束：** 牌堆抽干 + 有人出完手牌，注意：
   1. 一次性出完5张（如根号），不代表玩家游戏结束，要看牌堆是否已经抽干。

5. **结算：** 拳王分配剩余计分牌 → 统计当局得分 → 上缴换牌 → 下一局

### 一墩

1. 第一局第一墩，持有最小单张牌的玩家先手，且必须出携带该最小单张的牌型。
2. 非首局第一墩，由交出最大牌的输家为先手，牌型任意。
3. 非首局非首墩，上一墩的赢牌者先出牌（符合牌型的组合），并在公共池给玩家提醒出牌类型（5种牌型）
4. 下家跟牌（同牌型且更大）或过
5. 除一人外全过 → 该人赢下此墩，注意：
   - case1：先手出牌，中间有玩家跟牌，如：P1出牌，P2 P3过，P4出牌（压过 P1）
     - 压牌（出更大牌）会清零"连续过牌"计数，被压过去的玩家重新获得出牌机会。故 P4 压牌后需重新累计过牌：P1、P2、P3 依次过牌（期间若有人再压牌则计数再次清零），直到"除当前最大牌持有者 P4 外其余活跃玩家全部过牌"，P4 才赢下此墩；
     - 如果此时 P1 出牌压过 P4，则计数再次清零，P2 P3 P4 又各有一次新的出牌机会，以此类推。
6. 赢家收走桌上的计分牌（5/10/K）
7. 参与者补牌至 5 张，如果牌组数量不足，则按顺序优先补够赢家，再依次到下家。
8. 当卡牌被抽完，则为一局的最后一墩，须有玩家出完手牌，此墩才算结束。
   - case2：先手出牌，如果其余玩家均过牌，则先手须继续出牌，以此类推。
   - case3：先手出牌，如果中间有玩家出牌，如：P2 P3过，P4出牌，则P1可以选择过或者出牌
     - 如果P1 过牌，P2、P3 仍各需再过一次（过牌计数仅在压牌时清零）；待"除 P4 外其余活跃玩家全部过牌"本墩结束，P4 顶替 P1 作为先手继续出牌，参考case2
     - 如果P1 出牌压过 P4，则计数清零，P2 P3 P4 又各有新的出牌机会，以此类推



### 上缴+换牌（整局结束后）

> 口径说明：本节的"赢家（前 2）"**仅用于交粮配对**。积分榜与 🏆 胜局徽章只认**最终排名第一名**；第二位是**准赢家**，不额外记局胜。

1. 当局得分最高的 2 位是赢家，最低的 2 位是输家
2. 每个输家交出手中最大的单张
3. 第一赢家拿较大的牌，第二赢家拿较小的牌
4. 赢家各选 1 张单张还给对应输家
5. 交出最大牌的输家为下一局先手
6. <4 人模式：只涉及 1 输 1 赢

### 拳王

1. 一张一张分配未出计分牌
2. 参与者确认出拳（石头/剪刀/布）
3. 3-2-1 倒计时后揭晓
4. 输者出局，直到一人胜出
5. 胜者获得拳王徽章 + 该计分牌

### 分数并列

拳王决定谁优先。

---

## 5. Architecture

```
┌─────────────────────────────────────────────────┐
│  packages/client/    Vue 3 SPA                   │
│  路由 → RoomView / GameView / BoxerOverlay       │
│  Pinia store → useSocket / useGame / useBoxer    │
│                           │                      │
│  packages/engine/  纯函数库                        │
│  types.ts  deck.ts  judge.ts  compare.ts         │
│  score.ts  boxer.ts                              │
│                           │                      │
│  packages/server/   Express + Socket.io          │
│  REST API (房间 CRUD)                             │
│  WS 事件处理器                                    │
│  游戏状态机 (game-machine.ts)                      │
└─────────────────────────────────────────────────┘
```

**关键边界：** engine 是纯函数，零网络依赖。server 调用 engine 做判定。client 引用 engine 的类型获得类型安全。

---

## 6. Engine 层

### 文件结构

```
engine/src/
├── types.ts      # Card, Suit, Rank, HandType, Play
├── deck.ts       # createDeck, shuffle (Fisher-Yates), draw
├── judge.ts      # identify (识别牌型), beats (判定大小)
├── compare.ts    # 同牌型比较 (含花色)
├── score.ts      # 计分牌识别、当局得分计算
├── boxer.ts      # 拳王猜拳逻辑
└── index.ts      # 统一导出
```

### 核心类型

```typescript
enum Suit { Spade, Heart, Club, Diamond }
enum Rank { Four=0, Six, Eight, Ten, Jack, Queen, King, Ace, Three, Two, Five, Nine, Seven }

interface Card { suit: Suit; rank: Rank }

enum HandType { Single, Pair, Bike, Triple, Root }

interface Play {
  type: HandType
  cards: Card[]
  primaryRank: Rank
}
```

### 关键函数

```typescript
// deck
createDeck(playerCount: number): Card[]
shuffle(deck: Card[]): Card[]
draw(deck: Card[], n: number): { drawn: Card[]; deck: Card[] }

// judge
identify(cards: Card[]): Play | null    // null = 不合法
beats(newPlay: Play, currentBest: Play): boolean

// score
calculate(cards: Card[]): number
needsBoxer(unplayedCards: Card[]): Card[]

// boxer
resolveRound(moves: Map<pId, Move>): pId[]
finalWinner(survivors: pId[]): pId
```

### 测试

纯函数，`npx vitest` ~80 个测试用例。deck / judge / compare / score / boxer 各一个 spec。

---

## 7. Server 层

### 文件结构

```
server/src/
├── index.ts         # Express + Socket.io 启动
├── api.ts           # REST: GET/POST /rooms
├── ws.ts            # Socket.io 事件分发
├── room.ts          # 房间管理
├── game-machine.ts  # 游戏状态机
├── player.ts        # 玩家状态
└── utils.ts         # 日志、校验
```

### 游戏状态机

```
WAITING → DEALING → PLAYING ⇄ ROUND_END
                           ↘ SETTLING → WAITING
```

| 状态 | 事件 | 下一状态 | 服务端动作 |
|------|------|----------|-----------|
| WAITING | 全员ready | DEALING | 创建牌堆、洗牌、发5张 |
| DEALING | 动画完成 | PLAYING | 算最小单张者，通知 your_turn |
| PLAYING | 出牌 | ROUND_END | engine.judge → broadcast play_made |
| PLAYING | 过 | PLAYING | 通知下家 |
| ROUND_END | 补牌 | PLAYING/SETTLING | engine.draw → 判定是否局结束 |
| SETTLING | 拳王+换牌 | WAITING | 分配分数 → 广播结果 |

### Socket.io 事件

**客户端→服务端：** create_room, join_room, ready, play, pass, boxer_move, leave_room, reconnect

**服务端→客户端：** room_created, player_joined, game_started, your_turn(timeout=30), play_made, round_result, draw_card, game_over, boxer_start, boxer_reveal, boxer_winner, surrender_swap, next_game_lead, player_disconnected, player_reconnected

### 测试

集成测试：~15 个用例，用 socket.io-client 模拟 4 客户端。

---

## 8. Client 层

### 文件结构

```
client/src/
├── views/           HomeView / RoomView / GameView
├── components/
│   ├── game/        GameBoard / PlayerHand / PlayerSlot /
│   │                TableCards / ScoreDisplay / TurnIndicator / DeckInfo
│   ├── room/        CreateRoom / JoinRoom / PlayerList
│   ├── boxer/       BoxerOverlay / BoxerCountdown / BoxerReveal
│   └── common/      CardSprite / QuickChat / Toast
├── composables/     useSocket / useGame / useRoom / useBoxer
├── stores/          game.ts (Pinia)
└── assets/          cards/ sounds/
```

### 三个核心视图

1. **HomeView** — 创建/加入房间入口
2. **RoomView** — 等待所有人准备，显示房间码
3. **GameView** — 游戏主界面

### GameView 布局（手机竖屏）

```
┌─────────────────┐
│ 对家 (PlayerSlot) │
│ [?] [?] [?]     │
│                  │
│  桌中 TableCards  │
│  剩余: 32张      │
│  轮到: 玩家X     │
│                  │
│  分数榜           │
│                  │
│  我的手牌          │
│ [出牌]  [过]      │
└─────────────────┘
```

### 响应式

- < 768px：手机竖屏布局
- ≥ 768px：四人分坐四边
- `100svh` + `safe-area-inset-bottom` 防 iOS 遮挡

### BoxerOverlay

弹窗遮罩层：显示分配计分牌 → 参与者选拳 → 3-2-1 倒计时 → 揭晓 → 淘汰/胜出动画

---

## 9. 错误处理

### 三层防线

1. **Engine：** 非法输入抛 `GameError({code})`
2. **Server：** try-catch 所有事件，超时 30s 自动 pass，空房 10min 销毁，断线 30s 踢出
3. **Client：** 出牌前本地预校验，断连显示重连遮罩，错误 Toast 提示

### 边界情况

| 场景 | 处理 |
|------|------|
| 牌堆抽干但无人出完 | 继续打到有人出完 |
| 断线重连后已过回合 | full_state 恢复 |
| 断线 30s 被踢（游戏进行中） | 从 `game.players` 移除该座位；若被踢者正是当前出牌者或本墩最大牌持有者，则清空当前墩（桌上已出的牌保留，由下一墩赢家收走）并把出牌权交给下一位在场玩家；若处于拳王环节则从本轮淘汰并推进；房间名单同步移除该玩家 |
| 拳王仅剩1人 | 自动获胜 |
| <4人模式 | 上缴换牌 1v1 |
| 分数并列 | 拳王决定优先级 |

---

## 10. 测试策略

| 层 | 类型 | 工具 | 用例 | 覆盖率目标 |
|----|------|------|------|-----------|
| Engine | 单元 | Vitest | ~80 | ≥90% |
| Server | 集成 | Jest + socket.io-client | ~15 | 核心路径 |
| Client | 手动 | 4标签页自测 | - | 原型阶段不做 E2E |

---

## 11. 不做的事

- 无登录/注册（昵称即可）
- 无数据库持久化（重启丢失房间）
- 无大厅匹配（仅房间码加入）
- 无观战模式（原型阶段）
- 无表情包互动（仅快捷语）
- 无音效（原型阶段跳过）
- 无 CI/CD
- 无 Docker 部署（`npm run dev` 即可）

---

## 验证方式

原型完成后：同时打开 4 个浏览器标签页，加入同一房间，完整打完一局（发牌 → 多墩出牌 → 拳王 → 上缴换牌 → 下一局开始）。所有标签页状态一致，无报错。
