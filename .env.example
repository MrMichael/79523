# 复制为 .env 并填入真实值（.env 已被 .gitignore 忽略）

# 必填：JWT 签名密钥（生成：openssl rand -hex 32）
JWT_SECRET=

# 管理员账号（首次启动时若不存在则创建；之后改这里不会更新已有密码）
ADMIN_USERNAME=admin
ADMIN_PASSWORD=

# 必填：SakuraFrp 的 frpc 访问密钥（面板 -> 软件下载/配置 里获取，形如 xxxx:yyyy）
FRPC_TOKEN=
